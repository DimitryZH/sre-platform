prometheus:
  prometheusSpec:
    # Keep selectors aligned with observability/helm/kube-prometheus-stack/values.yaml
    serviceMonitorSelector:
      matchLabels:
        release: monitoring
    serviceMonitorNamespaceSelector:
      matchExpressions:
        - key: kubernetes.io/metadata.name
          operator: In
          values:
            - ingress-nginx
            - online-shop-dev
            - online-shop-stage

    ruleSelector:
      matchLabels:
        release: monitoring
    ruleNamespaceSelector:
      matchExpressions:
        - key: kubernetes.io/metadata.name
          operator: In
          values:
            - monitoring
            - online-shop-dev
            - online-shop-stage

# Avoid ArgoCD deadlocks on kube-prometheus admission hook Jobs/RBAC in bootstrap.
prometheusOperator:
  admissionWebhooks:
    # Keep webhook validation non-blocking while patch hooks are disabled.
    failurePolicy: Ignore
    patch:
      enabled: false
