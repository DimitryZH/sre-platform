# STAGE environment overlay (converged with validated dev baseline).
global:
  namespace: online-shop-stage
  domain: stage.example.com

frontend:
  replicaCount: 1
  ingress:
    # Keep catch-all ingress for direct LoadBalancer IP validation.
    useHost: false
  rollouts:
    enabled: true
  image:
    tag: v0.10.4

cart:
  replicaCount: 1
  image:
    tag: v0.10.4

checkout:
  replicaCount: 1
  image:
    tag: v0.10.4

payment:
  replicaCount: 1
  image:
    tag: v0.10.4

rollouts:
  enabled: true
