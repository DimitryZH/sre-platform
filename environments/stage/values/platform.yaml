# STAGE overlay
global:
  namespace: online-shop-stage
  domain: stage.example.com

# In Stage, we might want to test specific tags, but generally mirror prod.
frontend:
  replicaCount: 2

rollouts:
  analysis:
    prometheusAddress: http://monitoring-kube-prometheus-prometheus.monitoring.svc:9090