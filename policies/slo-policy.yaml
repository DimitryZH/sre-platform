service: frontend

slo:
  error_budget: 0.001
  window: 30d

release_policy:
  burn_rate:
    fast:
      threshold: 14
      window: 5m
    slow:
      threshold: 3
      window: 1h

  error_budget_remaining:
    minimum_percent: 20

  mode: strict
